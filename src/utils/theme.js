import { MD3LightTheme as DefaultTheme } from 'react-native-paper';

export default theme = {
    ...DefaultTheme,
    dark: true,
    colors: {
      ...DefaultTheme.colors,
      primary: 'tomato',
      secondary: 'yellow',
    },
  };